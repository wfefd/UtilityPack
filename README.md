## 1. 과제 소개: UtilityPack

`UtilityPack`은 팀원이 각각 구현한 유틸리티 기능 5가지를 하나의 Tkinter GUI 메뉴에서 선택해 실행할 수 있도록 만든 파이썬 프로그램입니다.

### 단위 변환기 (unit_converter_hyungkyu.py)

- 길이, 면적, 부피, 속도, 시간 등의 여러 단위를 서로 변환하는 도구  
- 카테고리별로 기본 단위(예: 길이 = m, 시간 = s)를 기준으로 변환 계수를 사용  
- 사용자가:
  - 카테고리 선택
  - 시작 단위 / 목표 단위 선택
  - 변환할 값 입력  
  → 변환 버튼 클릭 시 결과를 `"입력값 from = 결과값 to"` 형식으로 표시  
- Tkinter `OptionMenu`, `Entry`, `Label`, `Button`을 사용해 GUI로 구현

### 간단 계산기 (simple_calculator_jincheol.py)

- 두 숫자를 입력받아 사칙연산(+, -, ×, ÷)을 수행하는 간단 계산기
- 연산자 선택 후 결과를 화면에 출력
- 잘못된 입력(숫자가 아닌 값 등)에 대한 예외 처리 포함

### 점수 평균 계산 (score_average_harin.py)

- 여러 과목/시험 점수를 입력받아 평균 점수를 계산
- 공백으로 구분된 점수들을 입력 → 리스트로 변환 후 평균 계산
- 결과를 소수점 자리까지 보기 좋게 표시

### 문자열 처리 (string_tools_seunghwan.py)

- 문자열에 대해 다음과 같은 간단한 문자열 기능 제공
  - 대문자 / 소문자 변환
  - 공백 제거 또는 트리밍
  - 문자열 길이 확인 등
- 사용자가 문자열과 원하는 기능을 선택해서 실행할 수 있는 형태

### 랜덤 비밀번호 생성기 (password_generator_jngsu.py)

- 원하는 길이를 지정하면 랜덤 비밀번호를 생성해 주는 기능
- 사용 문자:
  - 영문 소문자 (a–z)
  - 영문 대문자 (A–Z)
  - 숫자 (0–9)
  - 특수문자 (`!@#$%^&*`)
- 길이를 입력하면 해당 길이의 랜덤 비밀번호를 만들어 팝업으로 보여줌


## 2. 역할 분담

- **공통 역할**
  - 메인 메뉴(`main_menu.py`) 설계 및 구현
  - Tkinter 기반 GUI 구조 합의
  - Git을 활용한 버전 관리 및 코드 리뷰

- **김형규**
  - `unit_converter_hyungkyu.py` 단위 변환기 기능 구현
  - 길이/면적/부피/속도/시간 단위 변환 로직 설계
  - Tkinter 기반 단위 변환 GUI 화면 구성

- **김승환**
  - `string_tools_seunghwan.py` 문자열 처리 기능 구현
  - 대소문자 변환, 문자열 관련 유틸리티 함수 작성
  - 메뉴와 연동되는 main() 인터페이스 정리

- **김하린**
  - `score_average_harin.py` 점수 평균 계산 기능 구현
  - 점수 입력 처리 및 평균 계산 로직 작성
  - 예외 처리(잘못된 입력값) 및 결과 출력 형식 구성

- **진철**
  - `simple_calculator_jincheol.py` 간단 계산기 기능 구현
  - 사칙연산 처리 로직 및 입력 검증
  - 결과 출력 UI 구성

- **변정수**
  - `password_generator_jngsu.py` 랜덤 비밀번호 생성기 기능 구현
  - 비밀번호 생성 로직(문자 조합, 길이 입력 처리) 작성
  - GUI 팝업을 통한 입력/출력 처리


## 3. 실행 방법

1. 저장소 클론

   ```bash
   git clone <레포지토리 URL>
   cd UtilityPack
